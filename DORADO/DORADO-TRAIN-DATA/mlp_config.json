{
    "window": 24,
    "epochs": 200,
    "batch_size": 64,
    "forecast_steps": 360,
    "forecast_days": 360,
    "time_unit": "day",
    "train_size": 510,
    "architecture": "Input(window,1) -> Flatten -> Dense(128,relu) -> Dense(64,relu) -> Dense(1,linear)"
}